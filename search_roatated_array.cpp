#include <iostream>
using namespace std;

int search_rotated_array(int arr[], int left, int right, int key)
{

    //    for(int i =0; i< arr.length(); i++){
    //        if(arr[i+1] > arr[i]){
    //            pivot = arr[i];
    //        }

    //    }

    int mid;
    
    if (right < left)
    {
        return -1;
    }
    mid = (left + right) / 2;

    if (arr[mid] >= arr[left] &&)
    {
    }
}
int main()
{
}